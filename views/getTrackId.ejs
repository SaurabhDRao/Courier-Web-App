<!DOCTYPE html>
<html>
  <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta http-equiv="X-UA-Compatible" content="ie=edge">
        <title>Instascan</title>
        <link rel="stylesheet" href="/static/materialize/css/materialize.min.css">
        <link href="/static/fontawesome-free-5.9.0-web/css/all.css" rel="stylesheet">
        <script type="text/javascript" src="/static/instascan.min.js" ></script>
  </head>

  <style>
        body {
            background: #0288D1;
        }

        #preview {
            width: 100%;
            height: 500px;
        }

        #track-container {
            background: #fff;
            text-align: center;
            border-radius: 50px;
            margin-top: 25px;
            margin-bottom: 25px;
        }

        video {
            margin-bottom: 20px;
        }
        
        .btn:hover {
            opacity: 0.8;
        }

        @media (max-width: 480px) {
            #track-contianer {
                padding: 0 20px;
            }
        }
  </style>

  <body>

    <header>
        <nav class="nav-wrapper blue darken-3">
            <div class="container">
    
                <a href="#" class="brand-logo">L-Courier</a>
    
                <a href="#" class="sidenav-trigger" data-target="mobile-links">
                    <i class="fa fa-bars"></i>
                </a>
    
                <ul class="right hide-on-med-and-down">
                    <li><a href="/home">Home</a></li>
                    <li><a href="/auth/login">Login</a></li>
                    <li><a href="/auth/getTrackId">Track</a></li>
                </ul> 
    
                <ul class="sidenav" id="mobile-links">
                    <li><a href="/home">Home</a></li>
                    <li><a href="/auth/login">Login</a></li>
                    <li><a href="/auth/getTrackId">Track</a></li>
                </ul>
    
            </div>
        </nav>
    </header>

    <div class = "container" id = "track-container">
        <div class="row">
            <form id = "trackForm" class="col s12" style = "margin-top: 20px;">
                <div class="row">
                    <div class="input-field col offset-m3 s8 m4">
                        <input id="trackId" type="text" class="validate" required = "">
                        <label for="trackId">Package ID</label>
                    </div>
                    <div class="input-field col s4 m4" style = "text-align: left">
                        <button class="btn waves-effect blue waves-light" type="submit" name="action">Track
                            <i class="fas fa-search-location"></i>
                        </button>
                    </div>
                </div>
            </form>
        </div>
        <video id="preview"></video>
    </div>

    <script src="/static/jquery.js"></script>
    <script src="/static/materialize/js/materialize.min.js"></script>
    <script src="/static/js/axios.min.js"></script>

    <script>
        $(document).ready(function(){
            $('.sidenav').sidenav();
        });
    </script>

    <script>
        let trackId = "";
        axios.defaults.headers.post['Content-Type'] ='application/x-www-form-urlencoded';
        let scanner = new Instascan.Scanner(
            {
                video: document.getElementById('preview'),
                mirror: false
            }
        );
        scanner.addListener('scan', function(content) {
            document.getElementById("trackId").value = content;
            trackId = content;
        });
        Instascan.Camera.getCameras().then(cameras => 
        {
            if(cameras.length > 0){
                scanner.start(cameras[0]);
            } else {
                console.error("Please enable Camera!");
            }
        });

        document.getElementById("trackForm").addEventListener("submit", (e) => {
            e.preventDefault();
            axios.post("/profile/update", { trackId: trackId }).then((res) => (console.log(res)));
        });  
    </script>

 </body>
</html>
</html>